[pytest]
addopts = --import-mode=importlib
pythonpath = .
testpaths = tests
markers =
    slow: spawns an isolated interpreter and stages the package (core-only acceptance)
    real_fsync: keep the real os.fsync/os.fdatasync (opts out of the autouse no_fsync fixture)
